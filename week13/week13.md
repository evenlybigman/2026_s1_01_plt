# 1day report - week13

## 8장 기억 장소 관리 -2

- 실행 시간 할당
  - interpreter언어: LISP, PYTHON, JAVASCRIPT
  - ALGOL 계열 언어
    - 정적 방식의 효율성 + 동적 방식의 융통성
    - 스택 방식으로 관리되는 부분에 기억장소 할당/해제 = 스택 동적 기억장소 관리
    - recursion 허용
- 스택 기반 기억 장소 관리
  - what?? : 스택 방식으로 관리되는 기억장소, 나중에 할당된 기억장소가 먼저 해제 됨
  - Algol계열 언어 => 블록 구조 - 선언문(새로운 환경 시작), 변수의 영역(space)
  - 단위 활성화(unit activation) : 블록의 코드부분과 활성 레코드를 위한 기억장소
    - code부분: 프로그램 실행될 때, 할당 받음 -> 프로그램 종료까지 해제되지 않음
    - activation record -> 블록 실행 시 할당, 실행 종료 시 해제(복구)
      - 동적 링크 = 동적 체인(CEP) / 정적 링크 / 복귀 주소(CIP) / 참조 환경
  - F -> G -> F
    - recursive call / F를 위한 활성 레코드가 있지만, F의 새로운 활성 레코드 블록 할당
  - 기억 장소의 생성과 소멸 과정
    - 나중에 생성된 활성 레코드가 먼저 소멸 됨 / stack 방식
  - 기억 장소 바인딩 : 기억 장소를 할당하여 실제 기억 장소의 위치를 확정 하는 것
  - 블록의 메모리 바인딩 = A.R.(활성 레코드) memory binding
    - 변수의 크기와 변위(offset)의 결정 시간에 따라
      - 정적 바인딩(static binding)
      - 활성화 시점 바인딩(binding on activation)
      - 동적 바인딩(dynamic binding)
- 비 지역 변수
  - 비지역 변수(non = local variables) 참조 : 다른 활성화 레코드에 있는 변수 참조
- 비지역 변수 탐색 방법 1: 정적 체인(static chain) 
  - 정적 내포 관계 표현 / 정적 체인을 이용한 비지역 변수 탐색 -> 실행 시간 낭비
- 비지역 변수 탐색 방법2: 체인 변위(chain offset = distance)
  - 블록의 정적 깊이(static depth)값을 가짐
  - 체인 변위: 정적 깊이의 차이 / 정적 깊이의 예: A = 0, B = 1, D = 2
  - 긴 거리의 경우 : 정적 체인을 여러번 따라가야 하므로 시간 낭비
- 비지역 변수 탐색 방법3: 디스플레이 (display)
  - 정적 체인 관계를 나타내는 배열
  - 장점: 모든 비지역 변수를 같은 시간에 참조할 수 있음(모든 비지역 변수를 찾는 시간이 동일함)
  - 단점: 활성화 레코드 생성 / 삭제 할때마다 display 값을 수정해야함

## 8장 기억 장소 관리 - 2

- Heap: 메모리 블록 / 자유롭게 할당(allocation) + 해제(de - allocation)
- 문제점
  - allocation, recovery, compaction, reuse등을 위한 기술이 필요
  - 기억 장소 수집 기술도 필요

- 힙의 필요성
  - 프로그램 실행중 아무떄나 메모리 할당/ 해제 할 수 있음
  - 프로그램의 어떤 위치에서나 데이터 구조 생성, 삭제 할 수 있음
- 힙 메모리 관리
  - 고정 크기 기억 장소 할당 -> 간단, no compaction -> 사용 가능 블록 리스트 유지
    - 복구 (recovery)
      - 프로그래머 or 시스템
      - free(), delete
      - garbage, dangling referece의 위험성
    - 참조 계수 : 기억장소에 대한 포인터 개수 = 허상 참조를 막음 / 추가의 공간이 필요함
    - 쓰레기 수집
      - 사용 가능 블록이 없을 때, 프로그램의 실행을 중단하고 쓰레기 수집을 시작
      - 2단계 : 마크, 청소
  -  가변 크기 기억 장소 할당
    - 복잡 / 어려움
    - 문제 : 사용가능 기억 장소 블록 리스트에 반환된 블록의 재사용
      - 부분압축 : 작은 블록을 합쳐 보다큰 블록을 만듬
      - 전체압축: 하나의 큰 블록을 만듬

